apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: managers-repo-build
spec:
  resources:
    inputs:
      - name: git-managers
        type: git
  params:
  - name: distBranch
    type: string
  - name: version
    type: string
  - name: extensionsVersion
    type: string
  - name: mavenCentralRepo
    type: string
    default: https://nexus.cics-ts.hur.hdclab.intranet.ibm.com/repository/proxy/
  - name: extensionsSourceRepo
    type: string
    default: https://repo.maven.apache.org/maven2/
  - name: dockerRepo
    type: string
    default: nexus.cics-ts.hur.hdclab.intranet.ibm.com:8080
  - name: noPush
    type: string
    default: ""
  - name: jacocoEnabled
    type: string
    default: "false"
  - name: isRelease
    type: string
    default: "false"
  steps:
  - name: gradle-build
    workingDir: /workspace/git-managers/galasa-managers-parent
    image: $(params.dockerRepo)/galasa/galasa-build
    command:
    - gradle
    - --no-daemon
    - --console 
    - plain
    - -PsourceMaven=$(params.extensionsSourceRepo)
    - -PcentralMaven=$(params.mavenCentralRepo)
    - -PtargetMaven=/workspace/git-managers/docker/repo
    - -PjacocoEnabled=$(params.jacocoEnabled)
    - -PisRelease=$(params.isRelease)
    - publish
  - name: docker-build
    workingDir: /workspace/git-managers/docker
    image: $(params.dockerRepo)/library/kaniko-executor:latest
    env:
    - name: DOCKER_CONFIG
      value: /tekton/home/.docker
    command:
    - /kaniko/executor
    - --dockerfile=./Dockerfile
    - --context=/workspace/git-managers/docker
    - --destination=$(params.dockerRepo)/galasadev/galasa-maven-managers:$(params.version)-$(params.distBranch)
    - --oci-layout-path=/workspace/git-managers/docker/image-digest
    - --skip-tls-verify
    - --skip-tls-verify-pull
    - --single-snapshot
    - --verbosity=info
    - $(params.noPush)
    - --build-arg=dockerRepository=$(params.dockerRepo)
    - --build-arg=extensionsVersion=$(params.extensionsVersion)
    - --build-arg=branch=$(params.distBranch)
    - --build-arg=gitHash=$(resources.inputs.git-managers.commit)
